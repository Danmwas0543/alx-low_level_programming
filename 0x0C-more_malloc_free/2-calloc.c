#include <stdlib.h>
#include "main.h"
/**
 * _memset - gives memory a constant byte of data
 * @s: to be supplied memory space
 * @b: character being replicated
 * @n: amount of duplicates to make of b
 * Return: a reference to the storage space
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int j;

	for (j = 0; j < n; j++)
	{
		s[j];
	}
	return (s);
}
