#include "main.h"
/**
 * _islower -> a fuction which checks for lowercase char
 * @c: char being evaluated
 * Return:0 if not lowercase 1 if it is
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
